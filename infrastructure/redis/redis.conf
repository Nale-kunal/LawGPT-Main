# Redis Configuration for Juriq
# Mount via: -v ./infrastructure/redis/redis.conf:/usr/local/etc/redis/redis.conf

maxmemory 256mb
maxmemory-policy allkeys-lru
save 60 1000
appendonly yes
appendfsync everysec
tcp-keepalive 300
timeout 0
loglevel notice
